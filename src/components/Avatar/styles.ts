import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
   container: {
      width: 51,
      height: 51,
      borderRadius: 8,
      alignItems: "center",
      justifyContent: "center",
      marginRight: 22,
   },

   avatar: {
      width: 47,
      height: 47,
      borderRadius: 8,
   }
});